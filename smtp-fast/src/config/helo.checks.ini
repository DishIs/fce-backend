# --- High-Speed HELO Checks Configuration ---
# We disable any check that requires a network lookup.

# These are slow DNS lookups.
reject_rdns_match=false
reject_forward_dns=false

# These are fast syntax/local checks that we can keep.
reject_bare_ip=true
reject_dynamic=true
reject_literal_mismatch=true