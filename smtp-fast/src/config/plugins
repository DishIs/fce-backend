# --- HIGH-SPEED PLUGIN CONFIGURATION ---

# CONNECT PHASE
# These run immediately upon connection. Keep this section minimal.
redis
tls
access

# HELO PHASE
# Basic HELO checks are fast.
# helo.checks

# MAIL FROM
# We are removing all DNS checks here (SPF, resolvable) for speed.

# RCPT TO
# These are essential for accepting mail for your domains.
# New - checks for pro user custom domains
rcpt_to_mongo 
rcpt_to.in_host_list

# DATA PHASE
# Lightweight plugins that operate on the email content.
# New - checks for muted senders
data.blocklist 
headers
limit
altinbox

# QUEUE
# Your custom Redis queue plugin.
# Now tiered
queue.redis 

# METRICS
# Your custom stats plugin.
stats.redis